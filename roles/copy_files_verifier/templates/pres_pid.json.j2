{
    "id": "{{ instance.id }}",
    "name": "{{ instance.name }}",
    "purpose": "{{ instance.purpose }}",
    "input_descriptors": [{
        "id": "{{ instance.id }}",
        "name": "{{ instance.name }}",
        "purpose": "{{ instance.purpose }}",
        "schema": [
            {
                "uri": "PID"
            }
        ],
        "constraints": {
            "fields": [
                {
                    "path": [
                        "$.credentialSubject.personal_administrative_number"
                    ]
                }
            ]
        }
    }]
}
