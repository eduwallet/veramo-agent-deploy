{
    "id": "{{ instance.id }}",
    "name": "{{ instance.name }}",
    "purpose": "{{ instance.purpose }}",
    "input_descriptors": [{
        "id": "{{ instance.id }}",
        "name": "{{ instance.name }}",
        "purpose": "{{ instance.purpose }}",
        "schema": [
            {
                "uri": "{{ instance.credential }}"
            }
        ],
        "constraints": {
            "fields": [
                {
                    "path": [
                        "$.credentialSubject.id"
                    ]
                }
            ]
        }
    }]
}
